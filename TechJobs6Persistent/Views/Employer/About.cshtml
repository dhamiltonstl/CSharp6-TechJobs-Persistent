@using TechJobs6Persistent.Models
@model Employer

@* @foreach(Employer employer in Model)
{ *@
    <h1>Employer: @Model.Id</h1>

    <div>
        <p>Name: @Model.Name</p>
        <p>Location: @Model.Location</p>
    </div>
@* } *@